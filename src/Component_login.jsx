import UserList from "./UserList";
import { useState } from "react";

function Login () {

    const [username, setUsername] = useState(""); // Guardamos el valor del username
    const [password, setPassword] = useState(""); // Guardamos el valor de la password
    const [message, setMessage] = useState(""); // Para mostrar mensajes (error o éxito)









    return (

    );

}